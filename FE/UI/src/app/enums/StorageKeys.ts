// UI\src\app\enums\storage-keys.enum.ts

/**
 * Enum for storing all localStorage keys used in the application
 * This centralized approach helps maintain consistency and prevents duplication
 * of storage keys across the application
 */
export enum StorageKeys {
  CART = "cart",
  USER_PREFERENCES = "userPreferences", // for future use
  THEME = "theme" // for future use
}
